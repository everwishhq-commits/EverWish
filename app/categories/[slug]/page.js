"use client";

console.log("🧪 Category page loaded, skipping API fetch for now");

export default function CategoryVideosPage() {
  return (
    <main className="flex flex-col items-center justify-center h-screen text-gray-700 bg-white">
      <h1 className="text-3xl font-bold mb-4">Category test page</h1>
      <p className="text-lg">✅ Page loaded — Splash working fine.</p>
    </main>
  );
}
