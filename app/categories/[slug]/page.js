console.log("🧪 Category page loaded, skipping API fetch for now");
export default function CategoryVideosPage() {
  return (
    <main className="flex flex-col items-center justify-center h-screen text-gray-700">
      <h1>Category test page</h1>
      <p>✅ Page loaded — Splash working fine.</p>
    </main>
  );
}
